import 'package:flutter/material.dart';

class OverduePage extends StatelessWidget {
  const OverduePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Overdue Page"),
      ),
    );
  }
}
